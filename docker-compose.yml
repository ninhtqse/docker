version: '3.7'
services:
  web:
    image: nginx
    ports:
      - "8099:80"
      - "443:443"
    volumes:
      - ./nginx:/etc/nginx/conf.d/:ro
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - php

  php:
    image: php:8.1-fpm
    volumes:
      - ./php:/var/www/html

volumes:
  ssl: